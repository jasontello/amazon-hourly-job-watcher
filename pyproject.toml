[project]
name = "amazon-hourly-job-watcher"
version = "1.0.0"
description = "Amazon hourly warehouse job watcher with ntfy push notifications"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
target-version = "py311"

